import { AuthResponseInterceptor } from './auth-response.interceptor';

describe('AuthResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new AuthResponseInterceptor()).toBeDefined();
  });
});
