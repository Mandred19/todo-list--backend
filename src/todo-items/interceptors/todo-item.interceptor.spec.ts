import { TodoItemInterceptor } from './todo-item.interceptor';

describe('TodoItemInterceptor', () => {
  it('should be defined', () => {
    expect(new TodoItemInterceptor()).toBeDefined();
  });
});
