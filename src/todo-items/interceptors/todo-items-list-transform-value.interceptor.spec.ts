import { TodoItemsListTransformValueInterceptor } from './todo-items-list-transform-value.interceptor';

describe('TodoItemsListTransformValueInterceptor', () => {
  it('should be defined', () => {
    expect(new TodoItemsListTransformValueInterceptor()).toBeDefined();
  });
});
