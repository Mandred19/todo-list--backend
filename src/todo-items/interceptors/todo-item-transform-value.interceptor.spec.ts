import { TodoItemTransformValueInterceptor } from './todo-item-transform-value.interceptor';

describe('TodoItemTransformValueInterceptor', () => {
  it('should be defined', () => {
    expect(new TodoItemTransformValueInterceptor()).toBeDefined();
  });
});
